export enum EFundRaiseStatus {
  ACCEPTING = 'accepting',
  VIP_PROGRESS = 'vip_progress',
  WHITELIST_PROGRESS = 'whitelist_progress',
  PUBLIC_PROGRESS = 'public_progress',
  OPENING = 'opening',
  CLOSED = 'closed',
  EXPIRED = 'expired',
}
